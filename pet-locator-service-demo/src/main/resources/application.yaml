---
server:
  port: 9091
 
spring:
  application:
    name: PET-LOCATOR-SERVICE 
    
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      definition: http://localhost:8761/eureka
  
  instance:
    hostname: localhost